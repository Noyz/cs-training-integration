$(document).ready(function(){
	$('.validate').on('click', function(){
		var ipString = $("input[type=text]").val();
		// var ipString = "10.10.10.17/26";

		var ipOrigin = ipString.split("/");
		var arrayIp = ipOrigin[0].split('.');
		var ipRangeValue = ipString.split(".");
				ipRangeValue = ipRangeValue[3].split('/');
				ipRangeValue = ipRangeValue[1];

			//Calculons le nombre d'Host
			var b = 32 - ipRangeValue;
			var host = Math.pow(2, b); 


			//si l'adresse est de classe C de type /25 à /32
			setTimeout(function(){
				// if(host < 256){
					for( var i = array[3] ; i < host; i++){
							arrayIp[3] = i;
							$('.result').append('<p>' + arrayIp.join('.')  + '</p>');
					}
				// }
			}, 0);	
	});
})
// soit une adresse CIDR : 


// var ipRangeValue = new Array();


// printf X.X.X.i++


// //adresse de type /24
// if(host=255)

// printf X.X.X.*


// //si l'adresse est de classe B de /16 à /23
// if(host>256 && host <= 65 536)

// var G=B-8
// var H=2^7
// for(var i=0; i++; i<H)

// printf X.X.X++.*






// //si l'adresse est de classe A de /1 à /16
// if(host>65 536)